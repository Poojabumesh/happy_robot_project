from load_matcher import load_available_loads

if __name__ == "__main__":
    results = load_available_loads(
        "data/loads.csv", 
        origin="Dallas", 
        destination="Chicago", 
        equipment_type="Dry Van"
    )

    if results:
        print("✅ Matching loads found:")
        for load in results:
            print(load)
    else:
        print("❌ No matching loads.")
